import { createContext } from 'react'

/* Creamos nuestro contexto, el cual es el que tenemos que consumir
para tener un manejo de configuraciones y de estado global de nuestra
aplicación */
export const CartContext = createContext()
