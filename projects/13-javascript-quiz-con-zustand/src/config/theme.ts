import { createTheme } from '@mui/material/styles'

/* La función createTheme nos permite crear temas personalizados para Material UI */
export const darkTheme = createTheme({
  palette: {
    mode: 'dark'
  }
})
